import 'package:flutter/material.dart';

/// A class that manages the color constants used in the application.
class ColorsManager {
  /// The main color used in the application.
  static const Color mainColor = Color(0xff00796B);

  /// The secondary color used in the application.
  static const Color secondColor = Color(0xff00d8b4);

  /// The primary color used in the application.
  static Color primaryColor = Color(0xff00796B);

  /// The white color used in the application.
  static Color whiteColor = Color(0xffFFFFFF);

  /// The black color used in the application.
  static Color blackColor = Color(0xff17181A);

  /// The grey color used in the application.
  static Color greyColor = Color(0xff5D6166);

  /// The brown color used in the application.
  static Color brownColor = Color(0xff7C7C7C);
  static Color grey = Color(0xff5A5A5A);
  static Color dragLine= Color(0xff141414);

  /// The color used for fields in the application.
  static Color fieldColor = Color(0xffF6F6F6);
  static Color green = Color(0xff00796B);
  static Color dottedLineColor = Color(0xffC8C7CC);

  /// The color used for the location pick-up marker.
  static Color locationPickUp = Colors.red;

  /// The color used for the location drop-off marker.
  static Color locationDropOff = Colors.green;

  /// The brown color used in the application.
  static Color orangeColor = Color(0xffFFBF00);

  /// The color used for the submitted review bottom sheet thank you text.
  static Color reviewThankYou = Color(0xff525252);

  /// The color used for Forward Arrows at trailing of ListTiles.
  static Color listTileArrowForward = Color(0xffBDBDBD);

  /// The color used for Forward Arrows at trailing of ListTiles.
  static Color hintColor = Color(0xffBDBDBD);

  /// The color used for divider color.
  static Color divider = Color(0xffEFEFEF);

  /// The color used for payment fields color.
  static Color paymentFields = Color(0xffEFEFEF);

  /// The color used for card discount.
  static Color cardDiscount = Color(0xff2679D5);
  static var scaffoldColor= Color(0xffF6F6F6);
}